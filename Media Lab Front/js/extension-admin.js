const extension=JSON.parse(localStorage.getItem('mlmExtensionRequest')||'null');if(extension&&document.querySelector('#requestList')){const panel=document.createElement('article');panel.className='request-card';panel.innerHTML=`<div class="request-head"><div><h2>Extension Request — ${extension.student}</h2><p>Booking ${extension.bookingId}</p></div><span class="status pending">Pending</span></div><div class="request-details"><article><b>Requested new return</b><p>${new Date(extension.requestedReturn).toLocaleString()}</p></article><article><b>Reason</b><p>${extension.reason}</p></article></div><div class="request-actions"><button class="reject">Reject Extension</button><button class="primary">Approve Extension</button></div>`;document.querySelector('#requestList').prepend(panel);panel.querySelector('.reject').onclick=()=>decide('Rejected');panel.querySelector('.primary').onclick=()=>decide('Approved');function decide(result){panel.querySelector('.status').className='status '+result.toLowerCase();panel.querySelector('.status').textContent=result;panel.querySelector('.request-actions').innerHTML='<b>Extension request updated</b>';localStorage.removeItem('mlmExtensionRequest')}}
